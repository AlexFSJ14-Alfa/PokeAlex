import { createContext } from "react";

export const PokemonContext = createContext()
// el contex nos permite hacer que provea una forma de pasar datos a traves del arbol de componentes sin tener que pasar props manualmente en cada nivel
